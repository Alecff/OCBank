{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    <h1> Register For an Account</h1>
    <h2>{{ error }}</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}

{% block title %}
    {{ parent() }} - Register
{% endblock %}

